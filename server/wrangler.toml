name = "vigolo-lead-radar-api"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# CORS: dominio del front (GitHub Pages / Vercel). "*" para pruebas.
[vars]
OPENAI_MODEL = "gpt-4o-mini"
ALLOWED_ORIGIN = "*"

# Secrets (NO se ponen acá; se cargan con `wrangler secret put`):
#   wrangler secret put GOOGLE_PLACES_API_KEY
#   wrangler secret put OPENAI_API_KEY
